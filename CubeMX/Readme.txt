These CubeMX project files were built retroactively to help compare implementation code to the source

Given this was built after the fact, not everything is captured here so do not rely on it as a source of information, it is just a troubleshooting tool. 